{% extends kimai_context.modalRequest ? 'form.html.twig' : 'base.html.twig' %}

{% block main %}
    {% if form is defined %}
        {{ include('@PeriodInsert/form.html.twig', {
            'template': kimai_context.modalRequest ? 'default/_form_modal.html.twig' : 'default/_form.html.twig',
            'title': 'periodinsert.subtitle'|trans,
            'form': form,
            'back': path(route_back)
        }) }}
    {% else %}
        Period Insert is not allowed in the [Time-clock] timetracking mode.
    {% endif %}
{% endblock %}